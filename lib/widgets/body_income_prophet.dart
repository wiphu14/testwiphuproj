import 'package:flutter/material.dart';

class BodyincomeProphet extends StatelessWidget {
  const BodyincomeProphet({
    super.key,
  });

  @override
  Widget build(BuildContext context) {
    return Text('income');
  }
}
