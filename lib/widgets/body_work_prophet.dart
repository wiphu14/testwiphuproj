import 'package:flutter/material.dart';

class BodyWorkProphet extends StatelessWidget {
  const BodyWorkProphet({
    super.key,
  });

  @override
  Widget build(BuildContext context) {
    return Text('Work');
  }
}